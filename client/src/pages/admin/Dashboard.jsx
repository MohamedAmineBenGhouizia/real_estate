const Dashboard = () => {
    return (
        <div>
            <h1>Admin Dashboard</h1>
            <p>Manage properties, reservations, and invoices here.</p>
        </div>
    );
};

export default Dashboard;
